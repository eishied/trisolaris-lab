# Experiments

Every simulation experiment receives a stable identifier such as `EXP-000001`.

A reproducible experiment preserves research question and hypothesis IDs, Git commit, dataset snapshots, model versions, configuration, random seeds, simulated time range, outputs, checksums, status and interpretation.

Experiment outputs are evidence candidates, not conclusions by themselves.
