name: Refresh science data

on:
  workflow_dispatch:
  schedule:
    - cron: "23 7 * * 1"
  push:
    branches: ["main"]
    paths:
      - "data/catalog/systems/ltt1445.json"
      - "pipelines/nasa_exoplanet/build_frontend_dataset.py"

permissions:
  contents: write

jobs:
  nasa:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build current LTT 1445 public dataset
        run: python pipelines/nasa_exoplanet/build_frontend_dataset.py

      - name: Commit dataset if changed
        shell: bash
        run: |
          if git diff --quiet -- frontend/data/ltt1445.json docs/data/ltt1445.json; then
            echo "No scientific-data changes detected."
            exit 0
          fi
          git config user.name "trisolaris-data-bot"
          git config user.email "actions@users.noreply.github.com"
          git add frontend/data/ltt1445.json docs/data/ltt1445.json
          git commit -m "data: refresh LTT 1445 NASA snapshot"
          git push
