# Provenance

This directory will contain machine-readable records linking:

`official source → raw snapshot → processed dataset → derived variable → model run → experiment → figure/table → scientific claim → publication`

No published result should be accepted without a complete provenance path.
